<p id="notice"><%= notice %></p>
<h1>Detalhes da Carona</h1>

<%= render partial: 'full_ride_card' %>

<% if @ride.driver_id == @logged_user.id %>
  <%= link_to 'Voltar', user_rides_path(@logged_user), class: 'btn btn-secondary' %>
<% else %>
  <%= link_to 'Voltar', search_rides_path, class: 'btn btn-secondary' %>
<% end %>